# 常见问题 FAQ

## 🚀 启动相关

### Q: 如何启动项目？

```bash
# 1. 安装依赖
pnpm install

# 2. 启动开发服务器（前端+后端）
pnpm -r run dev
```

### Q: 启动后无法访问？

**检查端口是否被占用**:

```bash
# 检查前端端口 5173
lsof -i :5173

# 检查后端端口 50000
lsof -i :50000
```

**解决方案**:
- 关闭占用端口的进程
- 或修改配置文件中的端口号

### Q: 后端连接失败 (ECONNREFUSED)?

**原因**: 后端服务未启动或启动较慢

**解决方案**:
1. 确认后端服务已启动（查看终端输出）
2. 等待后端完全启动后刷新页面
3. 检查 `apps/server/package.json` 中是否有 `dev` 脚本

### Q: 数据库连接失败？

**检查数据库配置**:

```bash
# 1. 确认 Docker 容器运行
docker ps

# 2. 启动数据库
docker-compose up -d

# 3. 运行数据库迁移
cd apps/server
pnpm prisma migrate dev
```

## 🎨 主题相关

### Q: 如何修改主题颜色？

访问 **后台管理 → 开发管理 → 组件画廊 → 皮肤配色**

或直接修改 `apps/web/src/config/theme.ts`:

```typescript
export const themeConfig = reactive({
  colors: {
    primary: '#03624C',    // 修改主色
    secondary: '#00cf3f',  // 修改辅助色
    accent: '#26d849',     // 修改强调色
  }
})
```

### Q: 主题配置不生效？

**检查步骤**:
1. 确认修改了 `themeConfig` 对象
2. 确认调用了 `applyTheme(themeConfig)`
3. 检查浏览器控制台是否有错误
4. 清除浏览器缓存并刷新

### Q: 如何导出/导入主题配置？

**导出**:
1. 访问组件画廊
2. 点击右上角"导出配置"按钮
3. 下载 JSON 文件

**导入**:
1. 访问组件画廊
2. 点击右上角"导入配置"按钮
3. 选择之前导出的 JSON 文件

### Q: 如何恢复默认主题？

访问组件画廊，点击右上角"恢复默认"按钮。

## 🧩 组件相关

### Q: 如何使用富文本编辑器？

```vue
<script setup>
import RichTextEditor from '@/components/RichTextEditor.vue'
import { ref } from 'vue'

const content = ref('')
</script>

<template>
  <RichTextEditor v-model="content" />
</template>
```

详见 [富文本编辑器文档](../02_组件使用/03_RichTextEditor富文本编辑器.md)

### Q: 富文本编辑器图片上传失败？

**默认使用 Base64 编码**:

```vue
<RichTextEditor v-model="content" />
```

**使用自定义上传**:

```vue
<script setup>
const handleUpload = async (file: File) => {
  const formData = new FormData()
  formData.append('file', file)
  
  const response = await fetch('/api/upload', {
    method: 'POST',
    body: formData,
  })
  
  const data = await response.json()
  return data.url  // 返回图片 URL
}
</script>

<template>
  <RichTextEditor 
    v-model="content" 
    :upload-image="handleUpload" 
  />
</template>
```

### Q: 如何自定义按钮样式？

**方式一：使用组件画廊**

访问 **组件画廊 → 基础控件 → 按钮**，可视化调整样式。

**方式二：修改 CSS 变量**

```css
:root {
  --btn-primary-bg-color: #03624C;
  --btn-primary-text-color: #ffffff;
  --btn-primary-border-color: #03624C;
}
```

**方式三：直接修改样式**

```vue
<style scoped>
.btn-primary {
  background-color: #03624C;
  color: #ffffff;
}
</style>
```

## 📁 文件相关

### Q: 如何添加新页面？

```bash
# 1. 创建页面组件
apps/web/src/views/admin/your-module/YourPage.vue

# 2. 添加路由
apps/web/src/router/index.ts

# 3. 添加菜单项
apps/web/src/layouts/AdminLayout.vue
```

详见 [项目结构文档](../01_规范指南/01_项目结构.md)

### Q: 如何添加新组件？

```bash
# 1. 创建组件文件
apps/web/src/components/YourComponent.vue

# 2. 导出组件（可选）
apps/web/src/components/index.ts

# 3. 使用组件
import YourComponent from '@/components/YourComponent.vue'
```

### Q: 文档存放在哪里？

所有文档存放在 `apps/web/src/docs/` 目录：

```
docs/
├── 01_规范指南/      # 项目规范
├── 02_组件使用/      # 组件文档
└── 03_常见问题/      # FAQ
```

## 🔧 开发相关

### Q: 如何调试代码？

**前端调试**:
1. 使用浏览器开发者工具
2. 在代码中添加 `console.log()`
3. 使用 Vue DevTools 浏览器扩展

**后端调试**:
1. 查看终端输出
2. 使用 `console.log()` 或 `Logger`
3. 使用 Postman 测试 API

### Q: 如何查看 API 请求？

**浏览器 Network 面板**:
1. 打开开发者工具 (F12)
2. 切换到 Network 标签
3. 刷新页面，查看所有请求

**后端日志**:
查看后端终端输出，所有请求都会被记录。

### Q: 如何添加新的 API？

**前端**:

```typescript
// apps/web/src/api/your-api.ts
export async function getYourData() {
  const response = await fetch('/api/your-endpoint')
  return response.json()
}
```

**后端**:

```typescript
// apps/server/src/your-module/your.controller.ts
@Controller('your-endpoint')
export class YourController {
  @Get()
  async getData() {
    return { message: 'Hello' }
  }
}
```

### Q: 如何修改数据库模型？

```bash
# 1. 修改 Prisma Schema
apps/server/prisma/schema.prisma

# 2. 生成迁移
cd apps/server
pnpm prisma migrate dev --name your_migration_name

# 3. 生成 Prisma Client
pnpm prisma generate
```

## 🐛 错误处理

### Q: 出现 TypeScript 类型错误？

```bash
# 重新生成类型定义
pnpm -r run build

# 或重启 TypeScript 服务器
# VS Code: Cmd+Shift+P → TypeScript: Restart TS Server
```

### Q: 样式不生效？

**检查步骤**:
1. 确认 CSS 文件已导入
2. 检查选择器优先级
3. 清除浏览器缓存
4. 检查是否使用了 `scoped` 样式

### Q: 组件不显示？

**检查步骤**:
1. 确认组件已正确导入
2. 检查组件名称是否正确
3. 查看浏览器控制台错误
4. 确认组件路径正确

### Q: 路由跳转失败？

**检查步骤**:
1. 确认路由已在 `router/index.ts` 中定义
2. 检查路由路径是否正确
3. 确认使用 `router.push()` 或 `<router-link>`

## 📦 部署相关

### Q: 如何构建生产版本？

```bash
# 构建前端
cd apps/web
pnpm build

# 构建后端
cd apps/server
pnpm build
```

### Q: 如何部署到服务器？

**前端**:
1. 构建生产版本: `pnpm build`
2. 将 `dist/` 目录上传到服务器
3. 配置 Nginx 或其他 Web 服务器

**后端**:
1. 构建生产版本: `pnpm build`
2. 上传代码到服务器
3. 安装依赖: `pnpm install --prod`
4. 启动服务: `pnpm start`

### Q: 如何使用 Docker 部署？

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

## 💡 最佳实践

### Q: 如何组织代码？

1. **按功能模块划分**: 相关文件放在同一目录
2. **组件就近原则**: 子组件放在父组件同级 `components/` 目录
3. **样式就近原则**: 组件样式优先使用 scoped style
4. **工具函数复用**: 提取公共逻辑到 `utils/` 目录

### Q: 如何命名文件？

- **组件文件**: PascalCase (如 `UserList.vue`)
- **工具文件**: camelCase (如 `theme.ts`)
- **样式文件**: kebab-case (如 `gallery.css`)
- **文档文件**: 数字前缀 + 描述 (如 `01_项目结构.md`)

### Q: 如何编写提交信息？

遵循 [Git 规范](../01_规范指南/02_Git规范.md):

```bash
feat: 添加新功能
fix: 修复 Bug
docs: 更新文档
style: 代码格式调整
refactor: 代码重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链相关
```

## 🔗 相关资源

- [项目结构](../01_规范指南/01_项目结构.md)
- [Git 规范](../01_规范指南/02_Git规范.md)
- [UI 设计规范](../01_规范指南/03_UI设计规范.md)
- [主题变量使用指南](../02_组件使用/02_主题变量使用指南.md)
- [组件设置系统](../02_组件使用/04_组件设置系统.md)

## 📮 获取帮助

如果以上内容无法解决你的问题：

1. 查看项目文档
2. 搜索 GitHub Issues
3. 提交新的 Issue
4. 联系项目维护者

---

**最后更新**: 2024-11-24
