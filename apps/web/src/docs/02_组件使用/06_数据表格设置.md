# 数据表格设置

数据表格设置提供了完整的表格样式定制功能，包括表头、行样式、边框和斑马纹等。

## 📋 功能特性

### 1. 表头样式
- ✅ 背景颜色设置
- ✅ 文字颜色设置
- ✅ 字体粗细调整（400-700）
- ✅ 实时预览效果

### 2. 行样式
- ✅ 行高调整（紧凑/标准/宽松/超宽松）
- ✅ 水平内边距设置
- ✅ 垂直内边距设置
- ✅ 悬停背景色设置
- ✅ 悬停效果实时预览

### 3. 边框样式
- ✅ 外围边框开关和样式设置
- ✅ 内部单元格边框开关和样式设置
- ✅ 边框宽度和颜色独立控制
- ✅ 表格圆角设置（5 个预设选项）

### 4. 斑马纹
- ✅ 开关控制
- ✅ 交替行颜色设置
- ✅ 提高表格可读性

### 5. 操作菜单
- ✅ 触发按钮大小调整
- ✅ 触发按钮颜色设置
- ✅ 悬停背景色设置
- ✅ 菜单背景色设置
- ✅ 菜单圆角设置
- ✅ 菜单项悬停色设置
- ✅ 实时预览效果

## 🎨 配置项说明

### 表头配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `headerBackgroundColor` | string | `#f9fafb` | 表头背景颜色 |
| `headerTextColor` | string | `#111827` | 表头文字颜色 |
| `headerFontWeight` | string | `600` | 表头字体粗细 |
| `headerFontSize` | string | `0.875rem` | 表头字体大小 |

### 行配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `rowHeight` | string | `3rem` | 行高 |
| `rowPaddingX` | string | `1rem` | 水平内边距 |
| `rowPaddingY` | string | `0.75rem` | 垂直内边距 |
| `rowTextColor` | string | `#111827` | 行文字颜色 |
| `rowFontSize` | string | `0.875rem` | 行字体大小 |
| `hoverRowBackgroundColor` | string | `#f3f4f6` | 悬停背景色 |

### 边框配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `outerBorder` | boolean | `true` | 是否显示外围边框 |
| `outerBorderWidth` | string | `1px` | 外围边框宽度 |
| `outerBorderColor` | string | `#e5e7eb` | 外围边框颜色 |
| `innerBorder` | boolean | `true` | 是否显示内部单元格边框 |
| `innerBorderWidth` | string | `1px` | 内部边框宽度 |
| `innerBorderColor` | string | `#e5e7eb` | 内部边框颜色 |
| `borderRadius` | string | `0.5rem` | 表格圆角大小 |

### 斑马纹配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `zebraStripe` | boolean | `false` | 是否启用斑马纹 |
| `zebraStripeColor` | string | `#f9fafb` | 交替行颜色 |

### 操作菜单配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `actionMenu.triggerSize` | string | `2rem` | 触发按钮大小 |
| `actionMenu.triggerColor` | string | `#6b7280` | 触发按钮颜色 |
| `actionMenu.triggerHoverBg` | string | `#f3f4f6` | 触发按钮悬停背景色 |
| `actionMenu.menuBg` | string | `#ffffff` | 菜单背景色 |
| `actionMenu.menuRadius` | string | `0.5rem` | 菜单圆角 |
| `actionMenu.itemFontSize` | string | `0.875rem` | 菜单项字号 |
| `actionMenu.itemFontWeight` | string | `400` | 菜单项字重 |
| `actionMenu.itemHoverBg` | string | `#f3f4f6` | 菜单项悬停背景色 |

## 🚀 使用方法

### 在组件画廊中设置

1. 访问 **组件画廊** 页面
2. 点击左侧菜单的 **数据表格**
3. 在左侧设置区域调整各项参数
4. 右侧实时预览查看效果
5. 点击 **保存配置** 应用到全局

### 在代码中使用

表格样式会自动应用到所有使用 `.table-wrapper` 类的表格元素。

#### 基础用法

```vue
<template>
  <table class="table-wrapper">
    <thead>
      <tr>
        <th>姓名</th>
        <th>邮箱</th>
        <th>角色</th>
      </tr>
    </thead>
    <tbody>
      <tr v-for="user in users" :key="user.id">
        <td>{{ user.name }}</td>
        <td>{{ user.email }}</td>
        <td>{{ user.role }}</td>
      </tr>
    </tbody>
  </table>
</template>

<style scoped>
.table-wrapper {
  width: 100%;
  border-collapse: collapse;
}

.table-wrapper thead tr {
  background-color: var(--table-header-bg);
  color: var(--table-header-color);
  font-weight: var(--table-header-weight);
}

.table-wrapper tbody tr {
  height: var(--table-row-height);
  transition: background-color 0.2s ease;
}

.table-wrapper tbody tr:hover {
  background-color: var(--table-hover-bg);
}

.table-wrapper th,
.table-wrapper td {
  padding: var(--table-padding-y) var(--table-padding-x);
  border-bottom: var(--table-border-width) solid var(--table-border-color);
  text-align: left;
}
</style>
```

#### 带斑马纹的表格

```vue
<template>
  <table class="table-wrapper table-striped">
    <thead>
      <tr>
        <th>产品名称</th>
        <th>价格</th>
        <th>库存</th>
      </tr>
    </thead>
    <tbody>
      <tr v-for="(product, index) in products" :key="product.id">
        <td>{{ product.name }}</td>
        <td>{{ product.price }}</td>
        <td>{{ product.stock }}</td>
      </tr>
    </tbody>
  </table>
</template>

<style scoped>
.table-striped tbody tr:nth-child(even) {
  background-color: var(--table-stripe-color);
}
</style>
```

## 💡 最佳实践

### 1. 行高选择

- **紧凑 (2.5rem)**: 适合数据密集型表格，需要在有限空间展示更多数据
- **标准 (3rem)**: 默认选项，适合大多数场景
- **宽松 (3.5rem)**: 适合内容较多或需要更好可读性的表格
- **超宽松 (4rem)**: 适合重要信息展示，强调视觉舒适度

### 2. 斑马纹使用

- ✅ **推荐使用场景**:
  - 列数较多的表格
  - 需要横向对比数据的表格
  - 数据密集型表格

- ❌ **不推荐使用场景**:
  - 列数很少（2-3列）的表格
  - 已有明显分隔线的表格
  - 设计风格极简的页面

### 3. 颜色搭配

- **表头背景**: 建议使用浅灰色系（如 `#f9fafb`），与内容区分明显
- **表头文字**: 建议使用深色（如 `#111827`），确保对比度
- **悬停背景**: 建议使用比表头稍浅的颜色（如 `#f3f4f6`）
- **斑马纹颜色**: 建议使用极浅的灰色（如 `#f9fafb`），不要过于突出

### 4. 边框设计

- **外围边框**:
  - ✅ 启用：表格有明确的容器边界，适合独立展示
  - ❌ 禁用：表格融入背景，适合嵌入式展示

- **内部边框**:
  - ✅ 启用：单元格之间有清晰分隔，适合数据密集型表格
  - ❌ 禁用：极简风格，需要配合斑马纹或悬停效果

- **圆角设置**:
  - **无圆角 (0)**: 传统表格风格，严肃正式
  - **小圆角 (0.25rem)**: 轻微柔和，保持专业感
  - **标准圆角 (0.5rem)**: 现代风格，推荐使用
  - **大圆角 (0.75rem)**: 友好亲和，适合用户端
  - **超大圆角 (1rem)**: 极简设计，强调视觉舒适

## 🎯 实际案例

### 案例 1: 用户列表表格

```typescript
// 配置
{
  table: {
    outerBorder: true,
    outerBorderWidth: '1px',
    outerBorderColor: '#e5e7eb',
    innerBorder: true,
    innerBorderWidth: '1px',
    innerBorderColor: '#e5e7eb',
    borderRadius: '0.5rem',
    headerBackgroundColor: '#f9fafb',
    headerTextColor: '#111827',
    headerFontWeight: '600',
    rowHeight: '3rem',
    rowPaddingX: '1rem',
    rowPaddingY: '0.75rem',
    zebraStripe: false,
    hoverRowBackgroundColor: '#f3f4f6',
  }
}
```

**特点**:
- 标准行高，适合常规浏览
- 无斑马纹，依靠悬停效果区分
- 清晰的内外边框，标准圆角

### 案例 2: 数据报表表格

```typescript
// 配置
{
  table: {
    outerBorder: true,
    outerBorderWidth: '2px',
    outerBorderColor: '#bae6fd',
    innerBorder: true,
    innerBorderWidth: '1px',
    innerBorderColor: '#bae6fd',
    borderRadius: '0.75rem',
    headerBackgroundColor: '#e0f2fe',
    headerTextColor: '#0c4a6e',
    headerFontWeight: '700',
    rowHeight: '2.5rem',
    rowPaddingX: '0.75rem',
    rowPaddingY: '0.5rem',
    zebraStripe: true,
    zebraStripeColor: '#f0f9ff',
    hoverRowBackgroundColor: '#e0f2fe',
  }
}
```

**特点**:
- 紧凑行高，展示更多数据
- 启用斑马纹，提高可读性
- 蓝色主题，适合数据分析场景
- 大圆角，友好视觉效果

### 案例 3: 极简风格表格

```typescript
// 配置
{
  table: {
    outerBorder: false,
    innerBorder: false,
    borderRadius: '0',
    headerBackgroundColor: 'transparent',
    headerTextColor: '#6b7280',
    headerFontWeight: '500',
    rowHeight: '3.5rem',
    rowPaddingX: '1.5rem',
    rowPaddingY: '1rem',
    zebraStripe: false,
    hoverRowBackgroundColor: '#fafafa',
  }
}
```

**特点**:
- 透明表头，极简设计
- 宽松行高，强调舒适度
- 无边框，完全融入背景
- 依靠悬停效果区分行

## 🔧 CSS 变量

表格设置会生成以下 CSS 变量，可在全局使用：

```css
:root {
  --table-header-bg: #f9fafb;
  --table-header-color: #111827;
  --table-header-weight: 600;
  --table-row-height: 3rem;
  --table-padding-x: 1rem;
  --table-padding-y: 0.75rem;
  --table-border-width: 1px;
  --table-border-color: #e5e7eb;
  --table-hover-bg: #f3f4f6;
  --table-stripe-color: #f9fafb;
}
```

## 📝 相关文档

- [基础控件设置](./04_基础控件设置.md)
- [全局配色](./01_全局配色.md)
- [UI 设计规范](../01_规范指南/03_UI设计规范.md)

---

**最后更新**: 2024-11-24

